# 柔和配色与连续编号

用户明确要求替换白色主色调并取消历史菜单编号。本次以新要求覆盖此前保留菜单编号的约定。

- 正文与输入提示使用低亮度灰蓝色（256 色索引 109），取消白色加粗；无 256 色能力的终端使用普通青色，NO_COLOR 和纯文本行为保留。
- 主菜单按显示顺序 1–19，SOCKS 助手按显示顺序 1–7，节点管理子菜单同样连续编号。
- 同步输入分派、默认退出选项、错误提示与 README；对主菜单所有操作和 SOCKS 菜单操作进行隔离分派验证，测试不调用真实服务操作。
- 重建发布文件及 SHA256，完整 Linux 测试通过后沿用当前 main 提交推送，核对线上文件与 CI。
