# 终端界面调整

按用户明确提出的界面与输入要求，沿用方案梳理、实施计划和完成前验证流程，在当前 main 分支完成。

设计：青蓝字符 Logo 与标题、白色正文和选项、绿色成功、黄色提醒、红色失败。首页按用途分组，保留 0–18 编号；核心、协议、证书选项逐行显示；向导按填写阶段分段。输入提示突出显示，密码、API Key、DNS 密钥填写时直接回显。无终端、TERM=dumb 或 NO_COLOR 时输出纯文本。继续保留日志和批量配置查看中的密钥脱敏。

- [x] 更新 `src/manager/common.sh`、`menu.sh` 的颜色、Logo、分组菜单与状态提示。
- [x] 更新 `config.sh`、`nodes.sh` 的逐行选择与分阶段向导。
- [x] 同步 `src/helper.sh` 的菜单和输入表现，移除关闭终端回显的处理。
- [x] 更新 README、终端回显测试，新增真实终端颜色与纯文本退化验证。
- [x] 生成发布脚本与 SHA256；运行完整 Linux 测试并查看实际终端输出预览。
- [x] 沿用本任务已确认的 main 发布方式，提交、推送并核对线上文件与 CI。

验证：Linux 完整测试 119 项无失败，跳过 1 项真实 systemd 测试；真实 PTY 验证输入回显、语义颜色、NO_COLOR 与 TERM=dumb。浏览器检查终端输出预览，字符边框改为普通 ASCII 并对齐。发布和 CI 结果以任务最终说明为准。
